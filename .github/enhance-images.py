from pathlib import Path
import re
import base64

# Sharp, high-resolution SOMSI presentation assets prepared from the approved
# SOMSI design previews. They replace the visibly degraded legacy JPEGs at build time.
ASSETS = {
    "realizace-1.jpg": "PLACEHOLDER"
}
